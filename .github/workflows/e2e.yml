name: E2E

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  e2e:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm ci

      - name: Run E2E tests (baseline)
        run: |
          echo "E2E tests not enabled yet."
          echo "Pipeline ready for Playwright/Cypress integration."
